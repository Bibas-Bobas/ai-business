# Core (training + inference + prepare)
numpy>=1.22,<2
torch>=2.0
torchvision>=0.15
rasterio>=1.3
Pillow>=9.0
tqdm>=4.64
matplotlib>=3.7

# Optional: real-history drift script (generate_drift_from_history.py)
opencv-python>=4.8
scikit-image>=0.21

# Install PyTorch with CUDA separately if needed, e.g. from:
# https://pytorch.org/get-started/locally/
